-- PetaPeta DeltaX Menu - Full ESP + Auto Features
-- Red/Black Theme | GUI Menu with toggles

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- Settings
local settings = {
    espPlayers = false,
    espMonsters = false,
    espItems = false,
    espChests = false,
    autoCollect = false,
    autoOpen = false,
    autoLoot = false,
    speed = 40,
    jumpPower = 80
}

-- GUI Menu
local gui = Instance.new("ScreenGui")
gui.Name = "PetaPetaMenu"
gui.Parent = LocalPlayer.PlayerGui
gui.ResetOnSpawn = false

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 250, 0, 320)
mainFrame.Position = UDim2.new(0.5, -125, 0.5, -160)
mainFrame.BackgroundColor3 = Color3.new(0, 0, 0)
mainFrame.BackgroundTransparency = 0.15
mainFrame.BorderSizePixel = 2
mainFrame.BorderColor3 = Color3.new(1, 0, 0)
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = gui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundColor3 = Color3.new(0.2, 0, 0)
title.Text = "PetaPeta Debug"
title.TextColor3 = Color3.new(1, 0, 0)
title.TextSize = 18
title.Font = Enum.Font.SourceSansBold
title.Parent = mainFrame

local function createToggle(text, yPos, settingKey, desc)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 25)
    frame.Position = UDim2.new(0, 5, 0, yPos)
    frame.BackgroundTransparency = 1
    frame.Parent = mainFrame
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.new(1, 1, 1)
    label.TextSize = 14
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.SourceSans
    label.Parent = frame
    
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.25, 0, 1, -4)
    btn.Position = UDim2.new(0.75, 0, 0, 2)
    btn.BackgroundColor3 = Color3.new(0.15, 0, 0)
    btn.BorderColor3 = Color3.new(1, 0, 0)
    btn.BorderSizePixel = 1
    btn.Text = "OFF"
    btn.TextColor3 = Color3.new(1, 0, 0)
    btn.TextSize = 12
    btn.Font = Enum.Font.SourceSansBold
    btn.Parent = frame
    
    btn.MouseButton1Click:Connect(function()
        settings[settingKey] = not settings[settingKey]
        btn.Text = settings[settingKey] and "ON" or "OFF"
        btn.TextColor3 = settings[settingKey] and Color3.new(0, 1, 0) or Color3.new(1, 0, 0)
        if not settings[settingKey] then 
            if settingKey:find("esp") then clearESP() end
        end
    end)
end

createToggle("ESP - Players", 35, "espPlayers")
createToggle("ESP - Monsters", 65, "espMonsters")
createToggle("ESP - Items (Tools)", 95, "espItems")
createToggle("ESP - Chests/Doors", 125, "espChests")
createToggle("Auto Collect (Items)", 155, "autoCollect")
createToggle("Auto Open (Chests/Doors)", 185, "autoOpen")
createToggle("Auto Loot (Coins/Money)", 215, "autoLoot")

-- Speed slider
local speedFrame = Instance.new("Frame")
speedFrame.Size = UDim2.new(1, -10, 0, 30)
speedFrame.Position = UDim2.new(0, 5, 0, 250)
speedFrame.BackgroundTransparency = 1
speedFrame.Parent = mainFrame

local speedLabel = Instance.new("TextLabel")
speedLabel.Size = UDim2.new(0.4, 0, 1, 0)
speedLabel.BackgroundTransparency = 1
speedLabel.Text = "Speed: 40"
speedLabel.TextColor3 = Color3.new(1, 1, 1)
speedLabel.TextSize = 14
speedLabel.TextXAlignment = Enum.TextXAlignment.Left
speedLabel.Font = Enum.Font.SourceSans
speedLabel.Parent = speedFrame

local speedBox = Instance.new("TextBox")
speedBox.Size = UDim2.new(0.4, 0, 1, -4)
speedBox.Position = UDim2.new(0.6, 0, 0, 2)
speedBox.BackgroundColor3 = Color3.new(0.1, 0, 0)
speedBox.BorderColor3 = Color3.new(1, 0, 0)
speedBox.BorderSizePixel = 1
speedBox.Text = "40"
speedBox.TextColor3 = Color3.new(1, 0, 0)
speedBox.TextSize = 14
speedBox.Font = Enum.Font.SourceSans
speedBox.Parent = speedFrame

speedBox.FocusLost:Connect(function()
    local val = tonumber(speedBox.Text)
    if val then
        settings.speed = val
        speedLabel.Text = "Speed: " .. val
    else
        speedBox.Text = tostring(settings.speed)
    end
end)

-- Jump slider
local jumpFrame = Instance.new("Frame")
jumpFrame.Size = UDim2.new(1, -10, 0, 30)
jumpFrame.Position = UDim2.new(0, 5, 0, 285)
jumpFrame.BackgroundTransparency = 1
jumpFrame.Parent = mainFrame

local jumpLabel = Instance.new("TextLabel")
jumpLabel.Size = UDim2.new(0.4, 0, 1, 0)
jumpLabel.BackgroundTransparency = 1
jumpLabel.Text = "Jump: 80"
jumpLabel.TextColor3 = Color3.new(1, 1, 1)
jumpLabel.TextSize = 14
jumpLabel.TextXAlignment = Enum.TextXAlignment.Left
jumpLabel.Font = Enum.Font.SourceSans
jumpLabel.Parent = jumpFrame

local jumpBox = Instance.new("TextBox")
jumpBox.Size = UDim2.new(0.4, 0, 1, -4)
jumpBox.Position = UDim2.new(0.6, 0, 0, 2)
jumpBox.BackgroundColor3 = Color3.new(0.1, 0, 0)
jumpBox.BorderColor3 = Color3.new(1, 0, 0)
jumpBox.BorderSizePixel = 1
jumpBox.Text = "80"
jumpBox.TextColor3 = Color3.new(1, 0, 0)
jumpBox.TextSize = 14
jumpBox.Font = Enum.Font.SourceSans
jumpBox.Parent = jumpFrame

jumpBox.FocusLost:Connect(function()
    local val = tonumber(jumpBox.Text)
    if val then
        settings.jumpPower = val
        jumpLabel.Text = "Jump: " .. val
    else
        jumpBox.Text = tostring(settings.jumpPower)
    end
end)

-- ESP Storage
local espList = {}
local function clearESP()
    for _, v in pairs(espList) do
        if v and v.Parent then v:Destroy() end
    end
    espList = {}
end

local function addESP(obj, color)
    if not obj or not obj.Parent then return end
    local box = Instance.new("BoxHandleAdornment")
    box.Size = obj.Size + Vector3.new(1, 1, 1)
    box.Color3 = color
    box.Transparency = 0.4
    box.AlwaysOnTop = true
    box.ZIndex = 999
    box.Adornee = obj
    box.Parent = obj
    table.insert(espList, box)
end

-- Main Loop
RunService.Heartbeat:Connect(function()
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChild("Humanoid")
    if not hrp or not hum then return end
    
    -- Speed & Jump
    hum.WalkSpeed = settings.speed
    hum.JumpPower = settings.jumpPower
    
    -- ESP
    if settings.espPlayers or settings.espMonsters or settings.espItems or settings.espChests then
        clearESP()
        
        -- Players (Red)
        if settings.espPlayers then
            for _, plr in pairs(Players:GetPlayers()) do
                if plr ~= LocalPlayer and plr.Character then
                    for _, part in pairs(plr.Character:GetDescendants()) do
                        if part:IsA("BasePart") then addESP(part, Color3.new(1, 0, 0)) end
                    end
                end
            end
        end
        
        -- Monsters (Orange)
        if settings.espMonsters then
            for _, model in pairs(workspace:GetDescendants()) do
                if model:IsA("Model") and (model.Name:lower():find("monster") or model.Name:lower():find("enemy") or model.Name:lower():find("npc") or model.Name:lower():find("boss")) then
                    for _, part in pairs(model:GetDescendants()) do
                        if part:IsA("BasePart") then addESP(part, Color3.new(1, 0.5, 0)) end
                    end
                end
            end
        end
        
        -- Items/Tools (Cyan)
        if settings.espItems then
            for _, item in pairs(workspace:GetDescendants()) do
                if item:IsA("Tool") and item:FindFirstChild("Handle") then
                    addESP(item.Handle, Color3.new(0, 1, 1))
                end
            end
        end
        
        -- Chests/Doors (Green)
        if settings.espChests then
            for _, part in pairs(workspace:GetDescendants()) do
                if part:IsA("BasePart") and (part.Name:lower():find("chest") or part.Name:lower():find("door") or part.Name:lower():find("crate") or part.Name:lower():find("vault")) then
                    addESP(part, Color3.new(0, 1, 0))
                end
            end
        end
    else
        clearESP()
    end
    
    -- Auto Collect Items
    if settings.autoCollect then
        for _, tool in pairs(workspace:GetDescendants()) do
            if tool:IsA("Tool") and tool:FindFirstChild("Handle") then
                local dist = (tool.Handle.Position - hrp.Position).Magnitude
                if dist < 12 then
                    firetouchinterest(hrp, tool.Handle, 0)
                    firetouchinterest(hrp, tool.Handle, 1)
                end
            end
        end
    end
    
    -- Auto Open Chests/Doors
    if settings.autoOpen then
        for _, detector in pairs(workspace:GetDescendants()) do
            if detector:IsA("ClickDetector") and detector.Parent then
                local p = detector.Parent
                local pos = p:IsA("BasePart") and p.Position or (p:FindFirstChild("Handle") and p.Handle.Position)
                if pos then
                    local dist = (pos - hrp.Position).Magnitude
                    if dist < 10 then
                        detector.MouseClick:Fire(LocalPlayer)
                    end
                end
            end
        end
    end
    
    -- Auto Loot (Coins/Money pickups)
    if settings.autoLoot then
        for _, part in pairs(workspace:GetDescendants()) do
            if part:IsA("BasePart") and (part.Name:lower():find("coin") or part.Name:lower():find("money") or part.Name:lower():find("gold") or part.Name:lower():find("gem")) then
                local dist = (part.Position - hrp.Position).Magnitude
                if dist < 10 then
                    firetouchinterest(hrp, part, 0)
                    firetouchinterest(hrp, part, 1)
                end
            end
        end
    end
end)

-- Toggle menu with F1
local menuVisible = true
UserInputService.InputBegan:Connect(function(inp, gp)
    if gp then return end
    if inp.KeyCode == Enum.KeyCode.F1 then
        menuVisible = not menuVisible
        mainFrame.Visible = menuVisible
    end
end)

print("PetaPeta Menu loaded! F1 to toggle menu")
print("ESP: Players, Monsters, Items, Chests | Auto: Collect, Open, Loot")